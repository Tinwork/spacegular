export interface CapsuleDetailOption {
    capsule_serial?: string,
    capsule_id?: number,
    status?: string,
    original_launch?: string,
    missions?: string,
    landings?: string,
    type?: string,
    details?: string,
}